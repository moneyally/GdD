{
  "name": "living-world-core",
  "version": "0.0.1",
  "private": true,
  "description": "Living World — CORE GAMEPLAY 증명. 지속 상태가 판단을 바꾸는지 검증하는 최소 구현.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "demo": "tsx src/cli/demo.ts",
    "check": "npm run typecheck && npm run test",
    "compare": "tsx src/cli/compare.ts",
    "tower": "tsx src/cli/tower.ts",
    "tower:stats": "tsx src/cli/towerStats.ts",
    "golden": "tsx src/cli/golden.ts",
    "gates:stats": "tsx src/cli/gateStats.ts",
    "prototype:build": "esbuild src/web/entry.ts --bundle --format=iife --global-name=LivingWorld --target=es2020 --outfile=prototype/engine.js --banner:js=\"/* 생성물 — 수정하지 말 것. 원본은 src/, 재생성은 npm run prototype:build */\""
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "esbuild": "^0.28.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
